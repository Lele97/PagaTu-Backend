server:
  port: ${SERVER_PORT:8083}

coffee:
  service:
    base-url: ${COFFEE_SERVICE_URL:lb://coffee}

auth:
  service:
    base-url: ${AUTH_SERVICE_URL:lb://auth}

app:
  frontend:
    invitationPath: /invitation
    resetPswPath: /resetPassword
    verifyEmailPath: /verify-email
    company: Paga-Tu
    domainUrl: ${DOMAIN_URL:http://localhost:8888}

spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false
  application:
    name: mail
  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:grandinetti.gabriele.mail@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:usjq uqwd jgfi zupt}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}

  # NATS Configuration
  nats:
    subject:
      next-payment: next-payment-subject
      skip-payment: skip-payment-subject
      invitation: invitation-subject
      reset-password-mail: reset-password-mail
      verify-email-mail: verify-email-mail
      pay-for: pay-for-topic
      invitation-response: invitation-response-subject
      turn-reminder: turn-reminder-subject
    server: ${NATS_SERVER:nats://localhost:4222}
    connection-timeout: 5000
    max-reconnect: -1
    connection-name: mail-service
  
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: ${EUREKA_INSTANCE_PREFER_IP:true}
    hostname: ${EUREKA_INSTANCE_HOSTNAME:}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method